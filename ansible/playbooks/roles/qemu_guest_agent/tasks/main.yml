---
- name: Install Qemu Guest Agent
  apt:
    name:
      - qemu-guest-agent
    state: present
    update_cache: yes

- name: Start Qemu Guest Agent
  shell: systemctl start qemu-guest-agent

- name: Enable Autostart Qemu Guest Agent
  shell: systemctl enable qemu-guest-agent
