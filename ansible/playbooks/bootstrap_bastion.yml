---
- name: Bootstrap Bastion host with ansible user
  hosts: bastion
  vars:
    ansible_pubkey_path: ~/.ssh/ansible.pub
  become: yes
  roles:
    - bootstrap_user
    - firewall
    # Removed because I don't need to gen ssh key here
    #- ssh_keygen
