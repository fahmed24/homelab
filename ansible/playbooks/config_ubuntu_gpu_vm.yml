- name: Install Nvidia Drivers
  hosts: k3s_nvidia_nodes
  vars:
    ansible_user: ansible
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
  become: yes
  roles:
    - ubuntu_nvidia_drivers
